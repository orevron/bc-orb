description: |
  scan repository with checkov to find security vonerabilities in your IaC files
usage:
  version: 2.1
  orbs:
    bc-orb: evrons/bc-orb@3.1.1
  jobs:
    build:
      machine: true
      steps:
        - bc-orb/scan:
            directory: "."
            suppress: true
            output: "json"
